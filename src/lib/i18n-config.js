/** Plain module (no "use client") so server components get real values, not client references. */
export const LOCALES = { en: "English", "zh-CN": "简体中文" };
export const LOCALE_COOKIE = "ap_locale";
